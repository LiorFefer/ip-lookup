import { colorosPalette } from "src/foundations";
import styled from "styled-components";

export const Header = styled.h1`
  font-size: 72px;
  font-weight: 700;
  color: ${colorosPalette.title};
  margin: 0;
`;
